# Semantic Depth Fixtures

Deterministic fixtures for GOV-021 producer-depth validation.

- `positive_candidate_pack.json` is a generic internal-only positive control.
- The historical weak candidate remains frozen at `../handoff_outcome/candidate_pack.json` and is used as the negative regression input.

These fixtures prove deterministic reviewability only. They do not constitute independent semantic Quality Pack approval, runtime authorization, production authorization, or automatic impact.
