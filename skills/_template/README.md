# LF Skill Pack Template

Status: GENERIC_REUSABLE_TEMPLATE / SANDBOX_PASS

This template defines the minimum structure for LF skill packs. It prevents basic prose-only outputs by requiring contracts, schemas, judges, checklists, examples, fixtures, validators, evals, handoffs and adapters.

## Required validation

Run:

```bash
python validators/validate_pack.py .
```
