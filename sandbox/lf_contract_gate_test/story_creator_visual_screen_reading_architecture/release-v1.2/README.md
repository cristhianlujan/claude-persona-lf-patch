# Story Creator — Visual Screen Reading and Auxiliary Context Reconciliation Architecture · Release v1.2

This release belongs only to **Story Creator**. `P0` is an internal pipeline stage code, not the project or package name.

The complete architecture body is stored as literal UTF-8 in the Supabase successor snapshot `STORY_CREATOR_VISUAL_SCREEN_READING_ARCHITECTURE` version `v1.2`. This GitHub folder publishes its immutable descriptor, exact hash/byte count, canonical manifest, strict RFC 8785 canonicalizer and read-only self-audit. The package is placed under the existing CI-approved sandbox prefix; no global allowlist is expanded.

```bash
python audit_story_creator_visual_screen_reading_release_v1_2.py
sha256sum -c STORY_CREATOR_VISUAL_SCREEN_READING_SHA256SUMS_v1.2.txt
```

Expected static result: `STATIC_SELF_AUDIT_PASS`. This does not authorize Task Packet, merge, runtime or production. A real GitHub Actions result and Supabase readback for the same final head remain mandatory.
