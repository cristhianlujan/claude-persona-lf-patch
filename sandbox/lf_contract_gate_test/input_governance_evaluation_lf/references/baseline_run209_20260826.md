# Baseline de calidad — run209 — 2026-08-26

**Tipo:** snapshot histórico de evaluación, NO fuente canónica.

**Propósito:** medir si una futura reparación mejora la calidad de remediación sin cambiar artificialmente el Story Gate.

## Readback directo

`programacion.fn_input_internal_remediation_summary(209)` reportó:

- 47 familias con resultado terminal de evaluación.
- 24 `POSITIVE`.
- 2 `NOT_APPLICABLE` con autoridad.
- 21 `NEGATIVE_CONFIRMED`.
- 30 propuestas/remediaciones validadas; 21 asociadas a negativos del corte actual.

Sobre las 21 remediaciones `NEGATIVE_CONFIRMED`, el readback estructural produjo:

| Propiedad requerida por Remediation Quality Contract v0.1 | Presente |
| --- | ---: |
| `exact_gap` | 0/21 |
| `evidence_found` como array | 0/21 |
| `do_not_do` no vacío | 0/21 |
| `close_when` no vacío | 0/21 |
| `next_owner` | 0/21 |
| `human_decision_required` explícito | 0/21 |

Además:

- 20/21 usan como `agent_action` una acción genérica equivalente a `KEEP_IN_INTERNAL_REMEDIATION_QUEUE` o `RESOLVE_PENDING_SOURCE_OR_EVIDENCE_INTERNALLY`.
- `FIELDS` y `VALIDATIONS` continúan como `NEGATIVE_CONFIRMED` aunque existe evidencia externa al resolver actual que exige investigación de falso negativo:
  - regla VIGENTE `REG_CLIENT_RECOVERY_PHONE_CONTROL_001`;
  - referencia explícita `old_phone_otp_field_code=CAMPO_OTP_CODE`;
  - campo `CAMPO_OTP_CODE` ACTIVO;
  - cinco validaciones ACTIVO asociadas al campo.

## Interpretación

El resultado terminal `NEGATIVE_CONFIRMED` es válido como descripción del corte actual, pero la remediación **no cumple aún** el contrato accionable v0.1.

Este baseline NO autoriza:

- convertir los 21 negativos a POSITIVE;
- asumir que FIELDS/VALIDATIONS ya están completos;
- crear fuentes nuevas;
- pedir Human Decision;
- modificar Story Gate.

## Target de mejora

Antes de integrar la nueva calidad de remediación al runtime:

- 21/21 negativos deben producir `exact_gap`, evidencia, acción concreta, `do_not_do`, `close_when`, owner y flag de Human Decision;
- 0 acciones terminales genéricas;
- 0 invenciones;
- 0 N/A sin autoridad;
- 0 Human Decision prematuras;
- FIELDS/VALIDATIONS deben atravesar las referencias existentes antes de cualquier creación/escalación;
- los cambios de resolver se evalúan después con successor obligatorio por ARC-015.
