# Scope freeze

This candidate contains exactly one solution: `GITHUB_PROVIDER_READBACK_V1`.

It does not implement Saga, migration reconciliation, resolver-registry DDL, merge, deployment, runtime activation, or production changes.
