# Story Creator — Visual Screen Reading Architecture · Release v1.1

This clean publication belongs only to **Story Creator**. `P0` is an internal pipeline stage code, not the project or package name.

The immutable architecture source is anchored in Supabase snapshot `9`; this repository folder publishes a descriptive index, strict RFC 8785 canonicalizer, deterministic self-audit, audit report, and checksums.

```bash
python audit_story_creator_visual_screen_reading_publication_v1_1.py
sha256sum -c STORY_CREATOR_VISUAL_SCREEN_READING_SHA256SUMS_v1.1.txt
```

Expected: `STATIC_SELF_AUDIT_PASS`. No workflow, canonical skill, runtime, migration, merge, production, or Task Packet authorization is included.
