# LF PR birth/update test

Todo PR debe pasar por el Router (ACT-0001) y traer manifiesto.

Esta instrucción existe únicamente en la rama base de prueba `test/pr-birth-20260924-base` para medir si el conector la entrega espontáneamente al crear o actualizar un PR.
