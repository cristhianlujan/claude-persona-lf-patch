# Remaining Live Runtime Gap

Package quality is addressed here. A separate canonical-runtime change is still required to resolve governed adapter bindings, compose only applicable capsules and persist `lf_adapter_invocations`. Until that execution surface exists and canaries pass, no live adapter invocation claim is allowed.
