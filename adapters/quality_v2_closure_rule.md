# Adapter V2 Closure Rule

A package may close as `CANDIDATE_CONTRACT_QUALITY_PASS` after deterministic and semantic candidate evidence passes. It may not close as live-runtime adapter PASS until canonical execution receipts prove applicable exactly-once invocation and zero adapter payload when not applicable.
