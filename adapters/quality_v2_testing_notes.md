# Adapter V2 Testing Notes

Deterministic tests should be runnable with Python standard library only where practical, so contract checks remain zero-cost and portable. Live model behavior is tested separately through canonical runtime canaries.
