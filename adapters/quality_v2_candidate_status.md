# Candidate Status Semantics

`CANDIDATE_READ_ONLY` means the package can be inspected and evaluated without implying operational enablement. Quality V2 can improve candidate evidence without changing runtime or production state.
