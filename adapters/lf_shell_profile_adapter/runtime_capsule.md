# ADAPTER_LF_SHELL_PROFILE — Runtime Capsule

Use only when Router/orchestrator resolves this adapter as applicable.

1. Resolve governed `screen -> module -> app_shell`; current Supabase LF authority outranks inherited Drive metadata.
2. Preserve specialist authority; adapter only constrains placement/binding.
3. Classify targets as `SHELL_LOCKED`, `SCREEN_COMPONENT`, or `SCREEN_SLOT`.
4. `SHELL_LOCKED` -> `RETURN_TO_ORCHESTRATOR_SHELL_CHANGE_REQUIRED`.
5. Never invent canonical tokens, routes, product rules, claims, CTA intent or financial meaning.
6. Missing material authority -> `RETURN_TO_ORCHESTRATOR_MISSING_AUTHORITY`.
7. Unresolved canonical conflict -> `BLOCKED_SOURCE_CONFLICT`.
8. Emit bounded `shell_binding` with canonical refs, protected/writable targets, normalized delta, precision basis, blockers and handoff.
9. Record exactly one matching `lf_adapter_invocations` entry.
10. No standalone execution, second model call, runtime enablement or production promotion.
