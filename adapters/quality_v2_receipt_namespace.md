# LF Adapter Receipt Namespace

Use `lf_adapter_invocations` exclusively for LF governed adapter applications. Do not reuse generic runtime fields such as `adapter_id` that may identify provider/model transport adapters.
