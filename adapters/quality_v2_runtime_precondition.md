# Runtime Proof Precondition

Before claiming live adapter quality, the canonical execution implementation itself must support governed adapter resolution and `lf_adapter_invocations` persistence. Package-level tests alone cannot satisfy this precondition.
