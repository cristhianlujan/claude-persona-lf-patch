# ADAPTER_PROJECT_BRAND_MOCKUP_RENDER_LF — Runtime Capsule

Use only when Router/orchestrator resolves a governed visual deliverable as applicable.

1. Resolve project -> Design System -> tokens -> screen specs -> route theme.
2. Governed project sources outrank generic templates/model defaults.
3. Never invent canonical palette, typography, spacing, route theme or frame.
4. Preserve upstream Product/UI semantics; adapter owns brand/render translation only.
5. Missing material brand authority -> `RETURN_TO_ORCHESTRATOR_MISSING_BRAND_AUTHORITY`.
6. Unresolved visual-source conflict -> `BLOCKED_VISUAL_SOURCE_CONFLICT`.
7. Emit bounded `render_binding` with refs, resolved tokens, frame policy, template, QA checks and blockers.
8. QA proves project identity, token provenance, frame integrity and no unsupported invention.
9. Record exactly one matching `lf_adapter_invocations` entry.
10. No standalone execution, second model call, runtime enablement or production promotion.
