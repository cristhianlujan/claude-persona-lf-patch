# Brand/Mockup Adapter Quality V2

This directory contains deterministic contract cases and the behavioral protocol for the project brand/mockup adapter.

`run_cases.py` is a contract regression suite. It does not execute the live LF runtime and cannot establish RAW adapter behavior.

Candidate contract PASS requires declared positive/negative cases, semantic review on material examples and compliance with the shared compact-capsule budget.
