# V11 root-tail closure lot

Scope: enforce root-level placement of `score`, `handoff_to_next`, and `self_verdict` after `deliverable_created` closes. Preserve all previously passing V10 semantics and bindings.
