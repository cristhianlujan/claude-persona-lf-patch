# UI Architect behavioral remediation V4 — evidence

Status: CANDIDATE / NO_RUNTIME / INDEPENDENT_SEMANTIC_AUDIT_PENDING

## Governance binding
- Profile asset: `PERFIL-UI-ARCHITECT`
- Operation: `ACTUALIZACION_PERFIL_LF`
- Execution: `EXEC-ACTUALIZACION-PERFIL-UI-ARCHITECT-20260827-001`
- Base: `main@7ec2b3a7e3b55b5b8c606cc01d726a2edae3bfb1`
- Candidate branch: `lf/ui-architect-remediation-v4-20260827`
- PR #238 is preserved as historical evidence only and is not the canonical write lineage for this execution.

The execution and `pre_write_execution_binding_gate` were recorded in Supabase before the first GitHub write on this candidate branch.

## Demonstrated BEFORE
1. Historical `UI_SECTION_SPEC` could receive a false 25/25 PASS despite incompatible output mode/rubric.
2. PR #238 validator V1 accepted PASS with 0/25.
3. V1 accepted nominal score evidence such as `PASS`/`ok`.
4. V1 accepted long generic remediation prose.
5. Deterministic structure alone accepted semantically wrong choices:
   - remove `payment_summary` while retaining the duplicate strip;
   - replace `Pago registrado` with `Deuda cancelada`.
6. Independent semantic audit found Ruta de Claridad dropped the upstream visible qualification that simulations are referential and subject to validation.
7. Fresh adversarials showed structurally plausible but harmful CTA relocation, guaranteed settlement copy, and Router/direct divergence.

## V4 change
- deterministic validator is total on malformed input;
- exact canonical score vocabulary and evidence structure;
- PASS threshold/verdict binding;
- score=5 structural support;
- machine-bound existing-screen actions (`operation/target/property/desired_value`);
- action evidence references real Component Tree IDs;
- category↔operation and operation↔acceptance compatibility;
- target-specific human implementation text;
- semantic authority routing metadata for meaning-changing actions;
- separate semantic judge against raw input/upstream authority;
- Router/direct consistency gate;
- Ruta AFTER visibly preserves `Simulación referencial sujeta a validación.`

## Local executed regression
`run_cases.py` executed against the exact V4 validator before publication.

Observed:
- 4/4 positive fixtures structurally PASS.
- 7/7 structural negatives REJECT.
- 4/4 malformed negatives REJECT without crash.
- 4/4 semantic adversarials deliberately STRUCTURAL PASS, proving semantic judge remains necessary.
- checkout direct and Router positive remediation actions are exactly equal.

The semantic adversarials are:
1. remove `payment_summary` instead of the duplicate strip;
2. invent `Deuda cancelada`;
3. move `payment_cta` farther from selection;
4. introduce `Liquidación garantizada al pagar`.

## Positive cases generated by the regression runner
The exact executable runner constructs and validates these positive artifacts:
- Ruta de Claridad AFTER, including visible `Simulación referencial sujeta a validación.` and one CTA to `/simulador`;
- checkout direct AFTER;
- checkout Router AFTER with identical remediation actions;
- marketplace mobile holdout preserving `Pago registrado` separately from receipt availability.

## Claim boundary
This lot may claim deterministic V4 regression PASS only after exact-head readback confirms the committed files.
It must not claim independent semantic PASS until a separate evaluator runs `judges/ui_architect_semantic_judge.md` against raw/upstream sources.
No runtime, VALIDATED, production promotion or merge is authorized by this evidence.
