# Mini Judge

Return `BLOCK_PIPELINE` if:

- Official impact is attempted without approval.
- Runtime or production general is enabled.
- Required schema is missing.

Return `RETURN_TO_WORKER_FOR_SELF_REPAIR` if:

- Output is prose-only.
- Required folders are missing.
- Evidence is missing.

Return `PASS_TO_QUALITY_PACK` only if all required files exist and validator passes.
